# Fusion-Model-Hub 产品需求文档 (PRD)

> 文档版本：1.0 | 生效日期：2026-07-16

---

## 一、产品定位

**macOS 原生、强绑定 Fusion-MLX 底座的本地化 AI 模型仓库与管理中心。**

Fusion-Model-Hub 是 Fusion 生态（Fusion-Desktop、Fusion-Agent Studio、Fusion-KB、Fusion-Bench）的**统一模型入口**，提供模型发现→一键下载→自动转换→版本管理→推理服务→生态对接全链路能力。

**核心约束：** 所有推理、转换、校验逻辑**100% 依赖 Fusion-MLX 底座**，无独立推理引擎，不兼容非 MLX 原生框架。

---

## 二、为什么需要 Fusion-Model-Hub？

### 2.1 当前痛点

| 痛点 | 影响 |
|------|------|
| 模型来源杂乱，无官方可信渠道 | 安全风险高，用户不敢下载 |
| 各组件分散管理模型，无统一版本控制 | 切换/回退困难，管理效率低 |
| 与 Fusion-MLX 底座耦合度不足 | 无法充分利用 Metal 加速与 KV Cache |
| 第三方模型（HF）适配 MLX 成本高 | 用户需手动转换，门槛高 |

### 2.2 解决方案

```
用户 → Fusion-Model-Hub → Fusion-MLX 底座 → Apple Silicon Metal
       │                        │
       ├─ 模型发现 / 下载       ├─ 推理引擎 (100% 依赖)
       ├─ 自动转换 / 量化       ├─ 哈希校验
       ├─ 版本管理              ├─ 格式转换
       └─ 生态对接              └─ KV Cache 优化
```

### 2.3 与 Fusion 生态关系

```
                    ┌──────────────────┐
                    │  Fusion-Desktop  │
                    └────────┬─────────┘
                             │
┌──────────────┐   ┌────────▼─────────┐   ┌──────────────┐
│  Fusion-KB   │◄──┤ Fusion-Model-Hub ├──►│ Fusion-Bench │
│  (知识库)    │   │   (模型管理中心)  │   │  (评测调优)  │
└──────────────┘   └────────┬─────────┘   └──────────────┘
                             │
                    ┌────────▼─────────┐
                    │ Fusion-Agent     │
                    │ Studio           │
                    └──────────────────┘
                             │
                    ┌────────▼─────────┐
                    │  Fusion-MLX 底座  │
                    │  (推理/转换/校验) │
                    └──────────────────┘
```

---

## 三、核心功能

### 3.1 模型仓库（官方 + 社区）

| 功能 | 说明 |
|------|------|
| 模型分类 | 按类型（对话/代码/多模态）、尺寸（7B/13B/34B）、量化（4bit/8bit/FP16）、设备（M1~M5） |
| 模型详情 | 展示 Fusion-MLX 版本、推理性能（tok/s）、内存占用、兼容组件、转换日志 |
| 来源管控 | 仅接入官方认证源，拒绝非 MLX 原生模型 |
| 搜索 | 名称/标签/参数模糊搜索，优先匹配 MLX 高适配模型 |

### 3.2 一键下载 + 自动校验

| 功能 | 说明 |
|------|------|
| 断点续传 | 支持 ≥10GB 模型断点续传 |
| 自动校验 | 下载后调用 Fusion-MLX 校验接口，核对哈希值 + 结构完整性 |
| 存储管理 | 默认 `~/Library/Fusion/Models`，支持自定义 |
| 版本锁定 | 下载时绑定当前 Fusion-MLX 底座版本 |

### 3.3 模型转换与量化（Fusion-MLX 内核独占）

| 功能 | 说明 |
|------|------|
| 源格式 | PyTorch（.bin/.safetensors）、Hugging Face |
| 转换引擎 | 直接调用 Fusion-MLX 转换接口，生成 `.mlx` 格式 |
| 量化选项 | 4bit/8bit（基于 GPTQ/AWQ，适配 M 系列芯片） |
| 批量转换 | 多模型队列转换，后台运行 |
| 转换日志 | 详细记录步骤、参数、耗时、内存占用 |

### 3.4 本地模型管理

| 功能 | 说明 |
|------|------|
| 模型列表 | 名称、版本、量化精度、内存占用、最后使用时间、Fusion-MLX 适配状态 |
| 快速操作 | 一键启动/停止、删除、重命名、复制 |
| 版本管理 | 多版本共存，一键回退 |
| 推理监控 | tok/s、内存占用、CPU/GPU 利用率 |
| 生态对接 | 一键授权给 Fusion-Desktop、Fusion-Agent Studio 等 |

### 3.5 Fusion-MLX 底座绑定

| 功能 | 说明 |
|------|------|
| 底座检测 | 启动时自动检测 Fusion-MLX 版本，未安装则提示 |
| 版本更新 | 检测新版本，更新后自动同步所有模型适配参数 |
| 兼容性修复 | 模型加载失败时调用 Fusion-MLX 修复接口 |
| 底座信息 | 展示版本、Metal 优化状态、KV Cache 支持情况 |

---

## 四、技术架构

```
┌─────────────────────────────────────────────────────────────────┐
│  表现层 (SwiftUI)                                               │
│  模型列表 · 详情页 · 下载管理 · 转换管理 · 设置面板             │
└────────────────────────────────┬────────────────────────────────┘
                                 │
┌────────────────────────────────▼────────────────────────────────┐
│  业务层 (Python)                                                │
│                                                                  │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────────┐  │
│  │  ModelRepo     │  │  Downloader   │  │  ModelConverter  │  │
│  │  仓库列表/搜索 │  │  断点续传/校验 │  │  HF→MLX 转换/量化│  │
│  └────────┬───────┘  └───────┬────────┘  └────────┬─────────┘  │
│           │                  │                    │            │
│  ┌────────▼──────────────────▼────────────────────▼─────────┐  │
│  │  LocalModelManager    底座绑定模块    生态对接模块        │  │
│  │  本地模型CRUD/版本/监控  检测/更新/修复  Desktop/Agent    │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────────────────────────────┬────────────────────────────────┘
                                 │ 100% 依赖 Fusion-MLX
┌────────────────────────────────▼────────────────────────────────┐
│  底座层 (Fusion-MLX)                                            │
│                                                                  │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────────┐  │
│  │  推理引擎      │  │  转换器       │  │  校验器          │  │
│  │  Metal/KV Cache│  │  HF→MLX 量化  │  │  哈希/结构/兼容  │  │
│  └────────────────┘  └────────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 技术约束

| 约束 | 说明 |
|------|------|
| 无独立推理引擎 | 所有推理 100% 依赖 Fusion-MLX 底座 |
| 格式强制校验 | 所有模型必须通过 Fusion-MLX 格式校验 |
| 接口独占 | 仅调用 Fusion-MLX 原生接口 |
| macOS 原生 | SwiftUI + Python 后端，适配 M1~M5 |

---

## 五、非功能需求

| 维度 | 指标 |
|------|------|
| 模型列表加载 | ≤ 1s（本地缓存） |
| 7B 模型转换 | ≤ 10min（M3） |
| 模型加载 | ≤ 2s（Fusion-Desktop 调用） |
| 推理速度 | 7B 4bit ≥ 20 tok/s（M3） |
| 内存占用 | 4bit 7B ≤ 8GB |
| 系统 | macOS 14+，M1~M5 |
| 底座 | Fusion-MLX ≥ v0.5.0 |
| 模型格式 | 仅 `.mlx` |
| 崩溃率 | ≤ 0.1% |

---

## 六、与现有 Fusion 项目的关系

| 项目 | 关系 |
|------|------|
| **Fusion-MLX** | 强制依赖底座，所有推理/转换/校验调用 Fusion-MLX 接口 |
| **Fusion-Desktop** | 提供模型列表，支持一键加载模型启动对话 |
| **Fusion-Agent Studio** | 提供模型服务地址，支持 Agent 调用 |
| **Fusion-KB** | 提供 Embedding 模型管理 |
| **Fusion-Bench** | 提供模型性能基准数据 |

---

## 七、Slogan

> **Fusion-Model-Hub — Fusion-MLX 生态唯一模型入口，让每一个模型在 Apple Silicon 上跑出极限性能。**